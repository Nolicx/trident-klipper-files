# Brush model: https://www.printables.com/model/957985-voron-24-bamboo-a1-brush-bucket

[gcode_macro CLEAN_NOZZLE]
description: Clean nozzle on brush. Supports wipe and circular scrub modes.
# Parameters:
#   PURGE=<mm>   — extrude this many mm before wiping (default: 0)
#   CLEAN=<°C>   — wait for nozzle to reach this temp before wiping (default: 0 = skip)
#   RETURN=<0|1> — raise Z to park_z_hop after cleaning (default: 1)
gcode:
    {% set v = printer["gcode_macro _VARIABLES"] %}
    # Runtime-tunable overrides — SAVE_VARIABLE VARIABLE=<name> VALUE=<x> to
    # change live, falls back to macros/_variables.cfg defaults otherwise
    {% set sv = printer.save_variables.variables %}
    {% set wipe_spd = sv.get('wipe_spd', v.wipe_spd) %}
    {% set accel_wipe = sv.get('accel_wipe', v.accel_wipe) %}
    {% set purge_spd = sv.get('purge_spd', v.purge_spd) %}
    {% set purge_ret = sv.get('purge_ret', v.purge_ret) %}
    {% set ooze_dwell = sv.get('ooze_dwell', v.ooze_dwell) %}
    {% set wipe_qty = sv.get('wipe_qty', v.wipe_qty) %}
    {% set wipe_return_mult = sv.get('wipe_return_mult', v.wipe_return_mult) %}
    {% set circle_radius = sv.get('circle_radius', v.circle_radius) %}
    {% set circle_spd = sv.get('circle_spd', v.circle_spd) %}
    {% set circle_passes = sv.get('circle_passes', v.circle_passes) %}
    {% set clean_do_wipe = sv.get('clean_do_wipe', v.clean_do_wipe) %}
    {% set clean_do_circular = sv.get('clean_do_circular', v.clean_do_circular) %}

    {% set nozzle_brush_y = printer.configfile.config["stepper_y"]["position_max"]|float %}
    {% set purge_len = params.PURGE|default(0)|int %}
    {% set clean_temp = params.CLEAN|default(0)|int %}
    {% set purge_temp = printer.extruder.temperature %}
    {% if clean_temp > 0 %}
        {% set purge_temp = clean_temp %}
    {% endif %}
    {% set old_accel = printer.toolhead.max_accel|float %}
    {% set do_return = params.RETURN|default(1)|int %}

    SAVE_GCODE_STATE NAME=clean_nozzle
    PARK
    SET_VELOCITY_LIMIT ACCEL={accel_wipe}

    # Purge filament before wiping if requested and nozzle is hot enough
    {% if (purge_temp > v.purge_temp_min) and (purge_len > 0) %}
        {% set clean_temp = purge_temp %}
        M107
        M104 S{purge_temp}
        TEMPERATURE_WAIT SENSOR=extruder MINIMUM={purge_temp}
        M83
        M106 S255
        G1 E{purge_len} F{purge_spd * 60}
        G1 E-{purge_ret} F{purge_spd * 60 * 5}
        G92 E0
    {% endif %}

    # Wait for nozzle to reach clean_temp if specified
    {% if clean_temp > 0 %}
        M104 S{clean_temp}
        TEMPERATURE_WAIT SENSOR=extruder MINIMUM={clean_temp}
        G4 P{ooze_dwell}
    {% endif %}

    {% if purge_len == 0 %}
        M106 S255
    {% endif %}

    ##################################################################
    # Wipe — approach brush each stroke, slow push into brush, fast snap back
    ##################################################################
    {% if clean_do_wipe %}
        {% for wipes in range(1, (wipe_qty + 1)) %}
            G0 Y{nozzle_brush_y - 14} F{v.travel_xy * 60}
            G0 X{v.brush_x_left}
            G0 Y{nozzle_brush_y}
            G1 X{v.brush_x_right} Y{nozzle_brush_y} F{wipe_spd * wipe_return_mult * 60}
            G1 X{v.brush_x_left} F{wipe_spd * 60}
            G1 X{v.brush_x_right} F{wipe_spd * wipe_return_mult * 60}
        {% endfor %}
    {% endif %}

    ##################################################################
    # Circular scrub — G3 CCW semicircles left→right across brush width
    # G3 with I=+r dips into -Y, stays within Y bounds
    # circle_passes controls number of full left→right sweeps
    ##################################################################
    {% if clean_do_circular %}
        {% set arc_d = circle_radius * 2 %}
        {% set n_arcs = ((v.brush_x_right - v.brush_x_left) / arc_d)|int %}
        {% for pass in range(circle_passes) %}
            G0 Y{nozzle_brush_y - 14} F{v.travel_xy * 60}
            G0 X{v.brush_x_left}
            G0 Y{nozzle_brush_y}
            {% set ns = namespace(x=v.brush_x_left) %}
            {% for i in range(n_arcs) %}
                G3 X{ns.x + arc_d} Y{nozzle_brush_y} I{circle_radius} J0 F{circle_spd * 60}
                {% set ns.x = ns.x + arc_d %}
            {% endfor %}
        {% endfor %}
    {% endif %}

    M107
    {% if do_return %}
        G0 Z{v.park_z_hop} F{v.travel_z * 60}
    {% endif %}
    SET_VELOCITY_LIMIT ACCEL={old_accel}
    RESTORE_GCODE_STATE NAME=clean_nozzle
